# MODULE 8 - WORKFLOW AUTOMATION

## Purpose

Allow operational automation.

Rule examples:

IF pod restart count > threshold
THEN generate incident

IF deployment failed
THEN rollback

IF node pressure detected
THEN notify SRE

Features:

* workflow builder
* trigger editor
* action editor

---

