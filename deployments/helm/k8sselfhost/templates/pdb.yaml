apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "k8sselfhost.fullname" . }}
  labels:
    {{- include "k8sselfhost.labels" . | nindent 4 }}
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      {{- include "k8sselfhost.selectorLabels" . | nindent 6 }}
